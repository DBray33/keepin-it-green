# Run the IndexNow ping on every deploy. The script always exits 0, so it can
# never fail the build. publish = "." matches this site's existing Netlify
# setup (static files served straight from the repo root).
[build]
  command = "node scripts/indexnow-ping.mjs"
  publish = "."

[build.environment]
  NODE_VERSION = "20"
